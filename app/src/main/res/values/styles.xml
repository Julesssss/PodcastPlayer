<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <!-- Set AppCompat’s color theming attrs -->
        <item name="colorPrimary">@color/mat_grey_dark</item>
        <item name="colorPrimaryDark">@color/mat_black</item>
        <item name="android:windowBackground">@color/mat_grey_mid</item>

    </style>

    <style name="AlertDialogCustom" parent="@android:style/Theme.Dialog">
        <item name="android:background">@color/mat_grey_light</item>
        <item name="android:textColor">@color/white_light</item>
    </style>

    <style name="CustomActionBarTheme"
        parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/MyActionBar</item>

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar"
        parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/material_deep</item>

        <!-- Support library compatibility -->
        <item name="background">@color/material_deep</item>
    </style>

</resources>
